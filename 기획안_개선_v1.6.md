# DB 돋보기 개선 기획안 v1.6

> **대상 프로그램**: DB 돋보기 (`oracle_guide_app.py`, v1.5 기준)
> **작성일**: 2026-09-09
> **성격**: 기획 문서 · 구현 제안

## 1. 현재 프로그램 진단 요약

| 구분 | 현황 |
|---|---|
| **메인 소스** | `oracle_guide_app.py` — 약 21,956줄 단일 파일 (클래스 62개) |
| **버전** | v1.5 (로컬 개발/테스트, GitHub는 MAJOR 변경 시만) |
| **DB** | SQLite `metadata.db` — 테이블 16종 (tables / columns / relations / queries / task_documents 등) |
| **핵심 기능** | 테이블 명세 조회·ERD·조인 SQL·대량 쿼리 생성·공통코드·업무문서·달력·명세서(xlsx/hwpx) 내보내기 |
| **현재 한계** | ① 실제 DB(Oracle)와 연동 없음 ② 명세 품질 자가 진단 수단 없음 ③ 명세 변경 이력/버전 관리 없음 ④ 단일 사용자 · 백업 수동 |

## 2. 개선 과제 (4개 카테고리 · 총 14개)

### A. 명세 품질 자가 진단 (데이터 거버넌스)

#### A1. 테이블 명세 완성도 대시보드 ⭐ 최우선 추천
- 테이블별 "작성 완성도 점수" 산출 (한글명 / 설명 / 컬럼 코멘트 / 데이터타입 / 관계 유무 등)
- 미비 항목 리스트: 컬럼 코멘트 없음, 타입·길이 미지정, PK 없는 테이블, 관계 미정의
- 시작 페이지 요약 카드 표시 → 클릭 시 해당 테이블로 이동

#### A2. 개인정보(개인식별정보) 컬럼 자동 탐지
- 컬럼명·코멘트 기반 패턴 매칭 (이름, 주민번호, 휴대폰, 주소, 계좌 등)
- 탐지 결과를 **마스킹 설정과 연동** → 내보내기 시 해당 컬럼 자동 제한 (공공기관 환경에 유효)

#### A3. 명세 오류 자동 점검
- 중복 테이블명/컬럼명, 관계 대상 테이블 부재(고아 관계), 커밋 기준 오타 프래그먼트 감지
- 점검 결과 리포트 `.txt / .xlsx` 저장

### B. 실무 DB(Oracle) 연동 → 명세 최신화

#### B1. 데이터 딕셔너리 동기화 ⭐ 매력적
- Oracle `ALL_TAB_COLUMNS`, `USER_TAB_COMMENTS` 등에서 테이블/컬럼/코멘트 조회
- **명세와 실 DB 차이 리포트** 생성 (신규 테이블, 누락 컬럼, 타입 불일치)
- 비파괴 방식으로 명세에 반영 (기존 마이그레이션 규칙 유지)

#### B2. 안전 쿼리 실행기
- 보관함 쿼리를 실제 DB에 실행 (기본 SELECT 전용, DML은 명시적 트랜잭션·롤백)
- 결과 그리드 표시 + CSV/엑셀 저장
- 커넥션 정보 암호화 저장, 연결 테스트 UI

#### B3. 실행 계획(EXPLAIN) 확인
- 인덱스 미스, 풀스캔 테이블 확인 → 운영 점검에 활용

### C. 변경 이력 · 백업 · 문서 산출물

#### C1. 명세 변경 이력 / 버전 관리 (가장 실질적)
- 테이블·컬럼 저장 시 이전 스냅샷 보관 → 누가/언제/무엇을 바꿨는지 이력 탭
- 변경 diff (추가/변경/삭제) 보기 → 이전 버전 복원

#### C2. 백업/복원 마법사 + 자동 백업
- 현재는 수동 (초기화 시에만 백업) → **백업 zip 생성 · 복원 · 자동 백업** (시작 시/주기)
- 기존 빈 호환 보호 정책 유지

#### C3. 명세서 템플릿 커스터마이즈 + 묶음 배포
- 내보내기 시 필드 선택·순서·회사/기관 로고 템플릿 저장/재사용
- 선택 테이블 묶음을 **xlsx + hwpx + 첨부 zip** 일괄 생성
- ⚠️ 기존 HWP/HWPX 내보내기 코드는 **수정하지 않고** 신규 템플릿 기능 **추가** 방식 + 회귀 테스트

### D. 업무 편의 · UX · 내부 품질

#### D1. 업무 문서 내용 검색 + 기한 알림
- 현재 제목 검색 위주 → **본문 내용 검색**
- 마감 예정/기한 초과 반복 업무를 시작 페이지·달력에 경고 표시

#### D2. 다크 모드 / 테마 전환
- PyQt6 스타일시트 구조가 있어 비교적 저렴하게 구현 가능

#### D3. 탭 세션 저장·복원
- 종료 시 열려 있던 탭 기록 → 다음 실행 시 복원

#### D4. 전역 검색 팔레트
- `Ctrl+K` → 테이블명/컬럼명/쿼리/업무 문서 통합 검색

#### D5. 대용량 성능 대비
- 테이블 1,000개+ 환경 대비 인덱스·페이징·지연 로딩 점검 (현재 20개 샘플 기준이라 유연)

#### D6. 코드 모듈 분리 (내부 품질) ⚠️ 신중 진행
- 21,956줄 단일 파일 → `ui/`, `db/`, `export/`, `widgets/` 구조로 분리
- HWP/HWPX 보호 규칙 준수를 위해 **순수 이동(동작 변경 없음)** 원칙, 별도 승인 후 진행

## 3. 제안 로드맵

| 단계 | 버전 | 포함 과제 |
|---|---|---|
| **1단계 (단기)** | **v1.6** | A1 완성도 대시보드, A2 개인정보 탐지, C2 백업 마법사·자동 백업, D1 본문 검색, D2 다크 모드 |
| **2단계 (중기)** | **v1.7** | C1 명세 변경 이력·diff, B1 딕셔너리 동기화, B2 안전 쿼 실행, C3 템플릿·묶음 배포, A3 오류 점검 |
| **3단계 (중장기)** | **v1.8** | D4 전역 검색, D3 탭 세션, B3 EXPLAIN, D5 성능, D6 모듈 분리 |
| **4단계 (배포)** | **v2.0** | 1~3단계 통합 후 GitHub 릴리스 (MAJOR 변경 조건) |

> 참고: v1.6~v1.8은 로컬 빌드/테스트용(기존 규칙), GitHub 공개 릴리스는 v2.0에서만.

## 4. 추천 우선순위 Top 3

1. **A1 테이블 명세 완성도 대시보드** — 구현 난이도 낮음, 명세 품질 점검에 즉시 효용
2. **C1 명세 변경 이력** — 명세 관리 툴의 본질적 가치(누가·언제·무엇을 바꿨나) 제공
3. **B1 데이터 딕셔너리 동기화** — "명세 ≠ 실 DB" 문제 해소, 차이 리포트로 설득력 높음

## 5. 구현 시 유의사항 (프로젝트 룰 적용)

- 같은 MAJOR 버전 내 변경(`1.5 → 1.6`)은 로컬 빌드/테스트용으로만 관리
- 기존 `metadata.db` 호환 유지, DB 변경 시 절대 경로로 백업 후 비파괴·반복 가능 마이그레이션
- 정상 동작 중인 **HWP/HWPX 내보내기 코드는 수정 금지**, `DB돋보기.spec`의 `collect_data_files('hwpx')` 보존
- 수정 후 실행 중인 프로그램 종료 → 재시작하여 핵심 화면 확인